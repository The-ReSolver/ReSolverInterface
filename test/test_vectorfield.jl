# This file contains the tests for the generic vector field implementation
# for a basic concrete implementation.

# a = VectorField(ScalarField(Grid()), ScalarField(Grid()))

# b = VectorField(ScalarField, Grid(), 3)
# c = VectorField(ScalarField, Grid(), x->x^2, x->cos(x))
# specialisevectorfieldconstructor(ScalarField)
# d = VectorField(Grid(), 4)
# e = VectorField(Grid(), x->x^2, x->cos(x))

# println(typeof(a))
# println(typeof(b))
# println(typeof(c))
# println(typeof(d))
# println(typeof(e))

@testset "Vector field constructor              " begin

end

@testset "Vector field array interface          " begin

end

@testset "Vector field broadcasting             " begin

end

@testset "Vector field grid methods             " begin

end

@testset "Vector field differentiation          " begin

end

@testset "Vector field inner-product            " begin

end
